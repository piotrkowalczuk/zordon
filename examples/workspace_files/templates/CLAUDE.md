# Workspace ${workspace.name}

This file was written by `zordon workspace create`, from
`examples/workspace_files/templates/CLAUDE.md`.

The stack is not running yet at that point, so nothing here can name a port or
any other resolved value. Ports live behind `zordon get` and the MCP tools.
